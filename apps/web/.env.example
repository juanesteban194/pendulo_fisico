# ─── apps/web · variables de entorno ─────────────────────────────────────────

# URL del API.
# Dev: http://localhost:4000 (el rewrite de Next lo dirige automáticamente)
# Prod (Vercel): URL pública de Railway, ej: https://pendulo-api.up.railway.app
NEXT_PUBLIC_API_URL=http://localhost:4000

# Modo: 'development' | 'production' | 'test' (Next lo setea automáticamente)
# NODE_ENV=development
